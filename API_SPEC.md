
This application was originally designed with a server integration layer. That integration has been removed by design decision.

The application is fully local and performs no network communication. No API endpoints are provided or required by this version. If server communication is needed in the future, the original API design can be reconstructed from the git history.
